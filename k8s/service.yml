apiVersion: v1
kind: Service
metadata:
  labels:
    service-label-key: nprdb-service-label
  name: nprdb-service
spec:
  type: ClusterIP
  ports:
    - name: service-port-name
      port: 80
      protocol: TCP
  selector:
    deployment-label-key: deployment-label-value
